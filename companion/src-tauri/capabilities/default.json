{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core access for the single popover window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
